# 课中音频整体策略（音效 SFX / BGM / TTS 人声指导）

| | |
|---|---|
| 版本 | v0.4（**§4 重构为「四大类话术 + importance×urgency 仲裁」体系**，对齐《课中语音指导与仲裁引擎》简化版；新增 §4.6 记忆与对比体系、§8.1 声音素材总表） |
| 日期 | 2026-08-28 |
| 范围 | Atom 端跳绳小程序**课中**（3·2·1 开跳 → 结算收尾）为主，兼及入口/结算页；首页阶段零语音（8-18 决议） |
| 相关 | [PRD §5.7 AI 教练语音](jump-rope-mini-prd.md) · [Demo](demo.html)（现行为基线，WebAudio 合成占位音） · 8-18 决议：**跳绳场景专属语音库**（动作提示/计数播报/趣味彩蛋）、**音效包新制**（不复用课程音效）、BGM 轻量可关 |

## 0. 一句话原则

**声音是这个产品的"第二块屏"。** 466 圆屏在 1–2 米外、用户全程"手里有绳"看不清小字——反馈的主通道是耳朵。因此：**计数音效永远第一优先**（它是"这一下算了"的唯一即时确认），人声其次，BGM 永远垫底；一切声音**短、低频、可关**，宁可少说不可打扰节奏。

## 1. 三层音频架构

| 层 | 内容 | 相对音量 | 打断关系 | 可关 |
|---|---|---|---|---|
| **L1 音效 SFX** | 计数叮 / 逢十重击 / 得星 / 倒计时哔 / 发令 / fanfare / 彩蛋 | 100%（基准） | 永不被打断，可与任意层叠放 | 不可单独关（关语音后仍保留——它是计数确认） |
| **L2 TTS 人声** | 报数 / 激励 / 指导 / 事件播报 / 流程语音 | ≈90% | 高优先级可打断低优先级（§4） | 可关（设置「教练语音」） |
| **L3 BGM** | 轻量节奏循环 | ≈25%，人声播报时 duck 到 12%（-50%） | 永不打断任何层 | 可关（设置「背景音乐」） |

混音要求：三通道独立增益；L2 说话时 L3 自动闪避（attack 80ms / release 400ms）；L1 不参与闪避。计数音效从 `onJump` 判定到出声 **< 100ms**（PRD §7 反馈延迟同帧要求）。

## 2. L1 音效包（新制，不复用课程音效——08-17 决议）

设计语言：**五声音阶（C 宫：C-D-E-G-A）统一调式**，BGM 同调式，任何叠放都不刺耳。全部为 ≤400ms 的干声短样本，预烘焙进固件资产包。

| id | 触发 | 时长 | 设计意图 | 与视觉同帧 |
|---|---|---|---|---|
| `sfx_jump` | 每次有效计数 | ~80ms | 五声音阶循环叮声（连跳时音高沿 C-D-E-G-A 步进，天然"越跳越high"） | 数字砸入动画 |
| `sfx_jump_heavy` | 逢十计数 | ~150ms | 重击版（低八度加厚），与数字重击帧同拍 | 重击闪 + 金光 |
| `sfx_star` | 热血2 内每保持 20 下得星（连击 50/70/90…，8-26 定稿） / 彩蛋 / 隐藏数字 | ~300ms | 上行琶音"叮铃"，奖励感 | 星星飞入 |
| `sfx_beep` | 倒计时 3·2·1 | ~120ms×3 | 干净短哔，最后一声可微升调 | 136px 大数字 |
| `sfx_go` | BodyPark 发令帧 | ~400ms | 有冲劲的上扬音（品牌声纹候选位） | 发令帧 0.75s |
| `sfx_fanfare` | 破纪录 / 达标 / 恭喜页 | ~600ms | 小号式短号角（全包里唯一"隆重"的一条） | 彩带 + 金光 |
| `sfx_timeup` | 限时到点 | ~400ms | 收束感下行双音 | 刻度环熄灭 |
| `sfx_tick_warn` | 限时最后 10s（可选逐秒） | ~60ms | 极轻的秒针声（待定：可能太吵，A/B） | 环转暖橙 |
| `sfx_tap` | 界面点按 | ~40ms | 极轻确认声 | — |

> 交付：设计侧出 9 条干声（48k/16bit 源，端上转 opus/pcm）；总资产预算 < 300KB。Demo 现用 WebAudio 合成版可作为**节奏与音高关系的听感 spec**直接参考。

## 3. L3 BGM（已拍板：跳频自适应变速）

- **形态**：8/16 小节无缝循环、五声音阶低音+轻打击，无旋律主线（不与人声抢频段，300Hz-2kHz 留给 TTS）。
- **跳频自适应 tempo（拍板 Q2）**：BGM 速度跟随实测跳频——
  - 输入 = 跳跃间隔 EMA（与虚拟绳同一转速信号源，天然同步）；
  - 映射：跳频 1.6–2.6 跳/秒 → **96–128 BPM**，两端 clamp；
  - **量化防抖**：±4 BPM 一档、只在小节边界切换、连续 2 小节达标才换档（避免速度"呼吸抖动"）；
  - **变速不变调**（time-stretch / 或合成器直接改 tick 间隔——Demo 的合成 BGM 即后者）；
  - 停跳/暂停 → 缓降回基准 110 BPM。
- **叠加"燃起来"强度分层**：基础层（0–49 下）→ +轻鼓组（50）→ +低音线（100）→ +高帽/energy（150+，封顶）。tempo 管"快慢"、layer 管"厚薄"，两轴独立。（注：视觉光芒已改为**热血两级**（热血1 轻微缓慢 / 热血2 热烈交替，与连击挂钩，8-26 定稿）；BGM 厚度仍按每 50 递进——一个表达"密度"、一个表达"里程"，不强绑定。）
- **落地节奏**：本周核心跑通期用**固定 110BPM 循环占位**（排期决议:核心先行），自适应引擎为本模块正式验收标准。
- **生命周期**：3·2·1 倒计时不放；发令帧起播；暂停即静音（不是降音量）；结算页停止；恭喜页不放 BGM 只放 fanfare。
- **闪避**：见 §1；连续播报（如整十连报）之间 <1s 不恢复，避免音量"呼吸"。

## 4. L2 TTS 人声：四大类话术体系 + 仲裁引擎（v0.4 重构）

> 框架来源：BodyPark ATOM《课中语音指导与仲裁引擎》跳绳简化版——**话术分四类 + 每句带 importance/紧迫度 + 简单粗暴仲裁**。事件只带"意图+参数"，措辞由话术库出、不写死在代码里；一个事件 ≥2 条变体随机选。

### 4.1 四大类话术（跳绳版落位）

| 大类 | 子类 | 跳绳事件 | 现状 |
|---|---|---|---|
| **🛡️ 安全**（最高，能打断一切；安静模式豁免） | 出框/机位 | 出画自动暂停 → **教练口吻引导**"回到画面里来，继续！"（不再是干巴巴的"已暂停"） | 🔧 改造 |
| | 负荷保护 | 30 分钟上限预告+强制结算；长轮（>20min）掉速时改关怀口吻"跳很久了，注意呼吸" | ✅ / 🔲 |
| | 危险动作 | 连续绊绳/趔趄 → "停一下，理理绳子再来" | V2（依赖识别） |
| **⏱️ 进程/节奏** | 计数 | 整十报数（纯数字整句，A 库） | ✅ |
| | 断绳/断连 | 高连击（≥30）断掉 → "没关系，重新连起来！" | 🔲 新增 |
| | 过渡 | 3·2·1·BodyPark 发令 / 时间到 / 暂停·继续 | ✅ |
| | 进度播报 | 限时剩 30s/10s；目标达成/再次满环；**目标追逐**（差 ≤10："还差 10 下就到目标！"）；结算小结 | ✅ / 🔲追逐 |
| **🎯 动作指导** | 前瞻引导 | 模式页一句；开场白召唤句；整 30 指导池（手腕摇绳/前脚掌/呼吸…） | ✅ |
| | 实时纠错 | formHint 四种（交替跳/单脚/跳太高/光线），20s 同类频控 | 💤 V1 休眠 |
| | 节奏纠正 | 掉速轻推"跟上 ATOM 的节奏"；（可选）越跳越快"稳住节奏" | ✅ / 🔲 |
| | 肯定确认 | 节奏稳定 20 下+ → "对，就是这个节奏！" | 🔲 新增 |
| **❤️ 情感/关系** | 激励冲刺 | 整 30 鼓励池；PB 追逐；最后 10 秒冲刺 | ✅ |
| | 安抚 | 断连安慰（同上）；空轮/未达标收尾"辛苦啦" | ✅ / 🔲 |
| | 里程碑庆祝 | 破纪录 / 连击星 / 热血全开 / 榜单前三·登顶 / 隐藏数字 | ✅ |
| | 寒暄/记忆 | 今日开场白（§4.5）；**记忆对比句（§4.6，"它记得我"）**；趣味彩蛋 | ✅ / 🔲对比 |

### 4.2 关键类型聚焦（v0.4 拍板方向：不求全，求出彩）

跳绳动作单一——**动作指导天然薄、安全类事件少，这是对的，不硬凑**。丰富度集中投在三个关键类型：

1. **记忆与对比**（❤️寒暄/记忆，§4.6）——"这产品真的记得我"的 wow 时刻，第一投入点；
2. **里程碑庆祝**（❤️庆祝）——连击/热血/破纪录/榜单的游戏化层，每个事件 ≥3 条变体；
3. **激励+安抚**（❤️激励/安抚）——鼓励池扩到 ≥15 条并分池（通用/连胜/高水平），补断连安慰。

骨架类（计数/过渡/出框安全）做到**稳、准、少**；纠错类（C2）保持休眠等识别能力；危险动作（S2）留 V2。

### 4.3 仲裁引擎（importance × urgency，替代旧 P0–P5）

每条候选话术带 `{importance(0–100), urgency, ttl, cooldownKey}`：

```
紧迫系数：NOW ×1.5 / SOON ×1.0 / WHENEVER ×0.6
priority = importance × 紧迫系数
正在播时：新句 priority − 当前句 priority > 抢占阈值(20) 才打断，否则丢弃（不排队）
```

**7 条规则**：①单句互斥（一次只说一句）②选最高 ③安全类 importance≈90 天然能打断一切 ④高出阈值才打断否则丢弃 ⑤过期（ttl）丢 ⑥同 cooldownKey 冷却防复读 ⑦**默认沉默**——除计数外话语类每分钟 ≤2 句，最后 10 秒冲刺窗口只留安全+过渡。

> 工程映射：Demo 现行 P0–P5 是本引擎的退化版（P0≈imp80×NOW，P4≈imp55×NOW 可丢弃）；端上按本表实现后 P 编号废弃。安静模式 = 只保安全类 + 发令/时间到。

### 4.4 话术交互清单（给程序填的总表；变体列 = 每语言最少条数）

> 逐条完整版（含全部文案变体与录制口径）已拆出独立文档 **[voice-script-list.md](voice-script-list.md)**（8-31）；本表保留仲裁参数概览。工程实现方案见 **[../code/voice-engine-spec.md](../code/voice-engine-spec.md)**。

| 触发事件 | 大类.子类 | imp | urg | 冷却 | 变体 | 生成档位 |
|---|---|---|---|---|---|---|
| 出框引导 | 安全.出框 | 90 | NOW | 3s | 2 | 固定 |
| 30min 上限预告/强制结算 | 安全.负荷 | 85 | NOW | — | 2 | 固定 |
| 长轮关怀（>20min 且掉速） | 安全.负荷 | 60 | SOON | 每轮1 | 2 | 固定 |
| 3·2·1 / BodyPark 发令 | 进程.过渡 | 75 | NOW | — | 1 | 固定（品牌声纹） |
| 时间到 / 结算小结 | 进程.过渡 | 72 | NOW | — | 3 | 模板+TTS（含数字与对比） |
| 破纪录 | 情感.庆祝 | 80 | NOW | 每轮1 | 3 | 固定+模板 |
| 达标 / 再次满环 | 进程.进度 | 70 | NOW | 每环1 | 3 | 固定 |
| 连击星 / 热血全开 | 情感.庆祝 | 65 | NOW | 机制自带 | 3 | 固定 |
| 限时剩 30s / 10s | 进程.进度 | 60 | NOW | — | 2 | 固定 |
| 榜单进前三/登顶 | 情感.庆祝 | 60 | SOON | 每轮1 | 2 | TTS（含昵称） |
| PB 追逐（差 ≤10） | 情感.激励 | 60 | SOON | 每轮1 | 3 | 模板 |
| 目标追逐（差 ≤10） | 进程.进度 | 55 | SOON | 每轮1 | 2 | 模板 |
| 整十报数 | 进程.计数 | 55 | NOW | 每次 | 1 | 固定（A 库 119 条） |
| 节奏轻推（掉速 60%+） | 动作.节奏 | 50 | SOON | 每轮1 | 3 | 固定 |
| 断连安慰（连击 ≥30 断） | 情感.安抚 | 45 | SOON | 10s·每轮≤2 | 3 | 固定 |
| 整 30 激励/指导 | 情感.激励+动作.引导 | 45 | SOON | 同条本轮不重复 | 15+6 | 固定 |
| 开场白（发令后 0–9s 专属窗） | 情感.寒暄 | 40 | SOON | 每轮1 | §4.5 | 云 TTS 预取 |
| 课中记忆对比（"超过昨天啦"） | 情感.记忆 | 35 | WHENEVER | 每轮1 | 模板×记忆点 | 模板 |
| 肯定确认（节奏稳 20 下+） | 动作.肯定 | 35 | WHENEVER | 20s·每轮≤2 | 3 | 固定 |
| 趣味彩蛋 | 情感.寒暄 | 25 | WHENEVER | 90s 窗·每轮≤3 | 20 | 固定 |
| 纠错四种（V1 休眠） | 动作.纠错 | 50 | SOON | 同类 20s | 4×2 | 固定 |

> 旧"内容六类 A–F"与生成方式分界不变：A 计数/B 节点/C 纠错/E 流程/F 彩蛋走**预生成整句**；含动态数字或昵称的（结算/PB 差值/榜单/记忆对比）走**模板+数字拼接或云 TTS**，弱网播无变量兜底句。

### 4.5 今日开场白（定稿：预生成 + 发令后播报）

**时机**：进入首页/模式页全程零语音；**3·2·1·BodyPark 发令后开播（边跳边听）**——开跳的前几秒事件最少，是天然的语音空窗，且绝不阻塞开跳。

**结构（2–3 句，总时长 ≤9s）**：

| 句位 | 内容 | 示例 |
|---|---|---|
| 句1 称呼欢迎 | 有昵称点名（未填不点名——脱敏决议）；当日首轮含"第 N 次跳绳" | "小雨，欢迎回来！这是你的第 18 次跳绳。" |
| 句2 数据亮点 ×1 | 连胜 / 昨日成绩 / 单轮 PB / 累计里程碑——按轮次轮换取 1 条 | "已经连续打卡 6 天啦。" |
| 句3 今日召唤 | **绑定所选模式**：自由跳带目标数（4 条池按日轮换）；限时 1'/2' 用冲刺/耐力召唤句 | "目标 200 下，出发！" / "一分钟限时，全力冲刺！" |

**预生成管线**：
- 内容当日确定 → **进入 APP（首页）时即拼装文本、请求云 TTS、缓存本地**；缓存 key = 日期 + 语言 + 数据快照 hash（当日中途数据变了自动重新生成）；
- 句 3 按 free / 1' / 2' 预生成三个变体，选模式后取对应条拼接；
- **就绪判定**：发令时音频已缓存 → 播；未就绪（弱网/TTS 慢）→ **本轮静默跳过，不等待、不补播**（后台继续生成，当日下一轮再用）；
- 完全离线 → 回落语音包里的通用开场句（"准备好了吗？今天也来一组！"）。

**轮次策略**：当日首轮完整版（3 句）；同日再次训练只播 1 句短版（"又来啦，今天已经 128 下，这轮继续冲！"）；结算页「再跳一轮」不播（无缝再战）。

**冲突仲裁**：开场白 imp40/SOON；播报期间整十报数让位丢弃（计数音效照常）；破纪录/达标/报星可打断开场白，被打断不续播。

### 4.6 记忆与对比体系（v0.4 新增——"我操，它真的记得我"）

**定位**：三个关键类型的第一位（§4.2）。目标是让用户在**每一轮**训练里至少遇到一次"它知道我以前怎么跳的"时刻——不只关注当下，还会讲以前。

**记忆字段**（端上档案 + 后端已具备，无新增采集）：

| 字段 | 示例记忆点 |
|---|---|
| 连胜 streak / 历史最长 maxStreak | "连续第 7 天，追平你的历史最长连胜" |
| 昨日/上次成绩 | "昨天跳了 180，今天试试超过它" |
| 单轮 PB + 达成日期 | "上个纪录保持了 12 天，今天被你打破了" |
| 最高连击 comboMax 历史 | "连击 45，比你之前最高多 8 个" |
| 累计 total / 训练次数 N / 首跳日期 | "这是你的第 50 次跳绳，累计破一万下" |
| 本周 vs 上周同期 | "这周已经比上周多跳 300 下" |
| 月榜名次变动 | "全球第 38，比昨天升了 6 位" |
| 习惯时段/回归间隔 | "三天没见，慢慢找回节奏就好" |

**四个出现位置**（全轮记忆点总量 ≤3，宁缺毋滥）：

| 位置 | 形式 | 状态 |
|---|---|---|
| ① 开场白句 2（§4.5） | 数据亮点轮换（已有，扩上表维度） | ✅ 扩容 |
| ② 课中动态对比 | 越过昨日成绩/上周纪录的**瞬间**播一句（imp35/WHENEVER/每轮1）——对比在事件发生那一刻说，才有"它在看着"的感觉 | 🔲 新增 |
| ③ 结算小结对比句 | 总结固定加 1 句对比："比昨天多 30 下""最高连击 45，历史新高" | 🔲 新增 |
| ④ 恭喜页纪录跨度 | 破纪录时报"跨度"："上个纪录保持了 12 天" | 🔲 新增 |

**动态选择规则**：每个位置从候选记忆点中取"**当天最特别**"的一条（优先级：今天恰好触发的里程碑 > 逼近历史极值 > 常规连胜/累计）；同一记忆点 48h 内不重复使用；无亮点时**宁可不说**，不硬凑（"记得住"的可信度来自克制）。

**生成档位**：记忆对比句 = **模板 + 数字拼接**（数字库已有）为主；结算复盘可升级云 TTS 整句（结算前数字已知，可预取）；弱网回落无变量兜底句。

## 5. 课中时间轴（自由跳 · goal 200 示例）

| 时刻/事件 | 层 | 内容 |
|---|---|---|
| 点 Start（模式页） | L2-E | 只播一句模式提示；**首页阶段零语音**（开场白已预生成待命） |
| 选模式 | L1 | `sfx_tap` |
| 倒计时 | L1+L2 | `sfx_beep`×3 + "三·二·一" |
| 发令 | L1+L2+L3 | `sfx_go` + "Body Park!"；BGM 起 |
| 发令后 0–9s | L2 | **今日开场白**（§4.5，预生成就绪才播；期间整十报数让位，P0–P2 可打断） |
| 每跳 | L1 | `sfx_jump`（音高步进） |
| 整 10 | L1+L2 | `sfx_jump_heavy` + 报数（P4） |
| 整 30 | L2 | 激励/指导五五开（P3，顶掉本次报数） |
| 整 50 | L1+L2 | `sfx_star` + "五十下，点亮第一颗星！"（P2）；BGM +1 层 |
| 差 PB ≤10 | L2 | "离你的最高纪录只差 N 下，冲！"（P2，一次） |
| 破 PB | L1+L2 | `sfx_fanfare` + "新纪录诞生！"（P0，打断一切） |
| 200 达标 | L1+L2 | `sfx_fanfare` + "目标达成！继续保持！"（P1）；环重置、BGM 保持最高层 |
| >3min 起 | L2-F | 趣味彩蛋窗口开启 |
| 29min | L2-E | "还有一分钟到三十分钟上限，冲刺！" |
| 暂停/继续 | L2-E | "已暂停"；BGM 静音 |
| 结算 | L2-E | 收尾总结（TTS 含数字）；BGM 停 |
| 点「完成」·新纪录 | L1+L2 | 恭喜页庆祝模块（吸纳 set-celebration 设计）：蓄力 riser ~260ms → 爆发 `sfx_fanfare`+「啪啪」叠层 → **t+300 语音「新纪录！」**（音效/人声错峰） |

限时 1'/2' 差分：+30s/10s 节点（"还有三十秒"/"最后十秒，冲刺！"）、最后 10s 可选 `sfx_tick_warn`、时间到 `sfx_timeup` + 总结（1' 含体测评级播报）。

## 6. 音色与人设（已拍板：单一元气教练，命名 ATOM）

- 人设锚点：**ATOM 的声音**（吉祥物/教练命名定稿 = **ATOM**，即设备自己的昵称；原工作名 Bono）——有活力、亲和、不"幼儿向"（主用户 6–17 岁 + 成人家庭，声音太低龄会劝退大孩子和家长）。
- **拍板（Q1）：单一「元气教练」**——有活力、亲和的青年声走全用户，试音后再评估是否扩展年龄段双音色。
- 双语：zh/en 同一人设两套录音/音色；EN 不做中式口音梗。
- 品牌声纹："Body Park!" 发令词值得单独精调（全产品可复用的 audio logo）。

## 7. 设置与可关性

- 「教练语音」关 = 关 L2 全部（含报数）；L1 音效保留（计数确认不可失去）。
- 「背景音乐」关 = 关 L3。
- **「安静模式」快捷档（拍板 Q4）**：设置页第 7 行一键开关 = 语音+BGM 双关、只留 L1 计数音效（夜间/公共场景）；实现即两开关的组合预设（Demo 已落地）。

## 8. 工程落地

> 端上仲裁引擎/记忆快照/TTS 管线的实现级方案见 [../code/voice-engine-spec.md](../code/voice-engine-spec.md)（8-31）。

### 8.1 声音素材总清单（v0.4 汇总，交付对账用）

| # | 素材组 | 条数/规格 | 形态 | 状态 |
|---|---|---|---|---|
| 1 | **SFX 音效包**（§2） | 9 条，≤400ms，五声音阶 C 宫，<300KB | 新制干声（Demo WebAudio 版=听感 spec） | 🔲 待设计侧交付 |
| 2 | **品牌声纹** "Body Park!" | 1 条（发令词，全产品可复用 audio logo） | 精调录音 | 🔲 待品牌侧 |
| 3 | **语音库·固定整句**（ATOM 元气教练，zh/en 双份） | A 计数 119 · B 激励 15+指导 6 · E 流程 ~15 · F 彩蛋 20 · 安全/安抚/肯定/断连（v0.4 新增）~16 ≈ **~190 条/语言** | 预生成 opus（≈3MB/语言） | 🔲 待录制（清单=§4.4 表） |
| 4 | **语音库·模板填槽** | 记忆对比模板 ~10 + PB/目标追逐 + 结算模板（槽=数字，拼 A 库数字音） | 模板+拼接 | 🔲 随 3 交付 |
| 5 | **动态云 TTS** | 开场白（§4.5 预取管线）/ 结算复盘 / 榜单昵称句 | 云合成+本地缓存，弱网兜底句 | 🔲 供应商待定（Q5） |
| 6 | **BGM** | 自适应循环 1 套（96–128 BPM 可变速，五声音阶，燃起来 4 强度层） | 可变速素材或端上合成（Q7） | 🔲 占位=固定 110 循环 |

- 资产：SFX 9 条（<300KB）+ 预生成语音包 ≈ **~190 条/语言**（v0.4 含新增安全/安抚/肯定类），opus 24kbps ≈ 3MB/语言。
- 端上：3 通道混音 + ducking + 优先级仲裁器（打断=立即停止当前 L2 源）；Demo 的 `speak()` 单通道行为即仲裁器退化版。
- TTS 引擎/供应商：待定（§9-Q5）；开场白与结算总结可**预取**（进结算前 500ms 内容已知）。
- 验收：计数音效延迟 <100ms；ducking 无爆音；任意 15 分钟连跳语音不重复感知（B/F 池轮换验证）。

## 9. 决策点

**已拍板（2026-08-18 会后）**：

| # | 问题 | 决议 |
|---|---|---|
| Q1 | 音色人设 | **单一「元气教练」**（青年声全用户，试音后再评估扩展） |
| Q2 | BGM 策略 | **跳频自适应变速**（96–128 BPM 跟随跳频，量化防抖、变速不变调；核心跑通期以固定 110 循环占位）+ 燃起来强度分层 |
| Q3 | 整十报数生成 | **整十句全预生成**（10–990 共 99 条 + 千位整百 20 条 / 语言） |
| Q4 | 安静模式 | **要**——设置第 7 行一键 = 语音+BGM 双关只留计数音效（Demo 已落地） |

**已拍板（2026-08-28，v0.4 框架评审）**：

| # | 问题 | 决议 |
|---|---|---|
| Q8 | 关键类型聚焦 | **记忆对比 + 游戏化庆祝/鼓励**——跳绳动作指导本身简单，做不了太专业具体；产品定位就是鼓励用户"动一动"，丰富度全投情感/关系大类 |
| Q9 | 记忆对比覆盖 | **头-中-尾三段全覆盖**：开场白亮点/回归关怀 → 课中越过昨天成绩的瞬间提醒（峰值体验）→ 结算小结对比句（课后小结报告形态后续可扩展） |
| Q10 | 记忆句生成档位 | **混合**：课中要快用模板+数字拼接；结算不赶时间用云 TTS 整句（弱网兜底句） |
| Q11 | 出框语音 | 出画自动暂停改**教练口吻引导**"回到画面里来，继续！"（安全类 imp90，安静模式豁免）；手动点按暂停仍用"已暂停" |

> v0.4 已在 Demo 落地：出框引导、课中越昨对比、结算对比句、恭喜页纪录跨度、回归关怀开场白、目标追逐、肯定确认、断连安慰、长轮关怀、鼓励池 7→15。

**已拍板（2026-08-31）**：

| # | 问题 | 决议 |
|---|---|---|
| Q12 | 播报逻辑部署位置 | **全量端上**——仲裁/冷却/限次/变体选择全部设备端离线可算;云端仅做开轮前记忆快照、动态句 TTS 预生成、语音包 OTA 三件慢事,断网课中语音零影响(实现见 code/voice-engine-spec.md §0/§1.1 逻辑树) |

**仍开放**：

| # | 问题 | 倾向 |
|---|---|---|
| Q5 | TTS 供应商与定制音色预算 | 一期系统 TTS + 预生成包兜底，定制音色二期 |
| Q6 | `sfx_tick_warn` 最后 10s 逐秒声会不会太吵 | 做出来 A/B，默认关 |
| Q7 | 自适应 BGM 的正式音源制作（需可变速素材 or 端上合成） | 与音效包一起委托设计侧，先用 Demo 合成器验证参数 |
